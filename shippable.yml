language: node_js

node_js:
  - 0.10.35

after_success:
  - if [ "$BRANCH" == "master" ]; then git remote add dokku dokku@45.55.46.229:vakitajs; fi
  - if [ "$BRANCH" == "master" ]; then git push dokku master; fi